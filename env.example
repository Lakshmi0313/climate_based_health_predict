# ══════════════════════════════════════════════════════
#  ClimateHealth AI — Environment Variables
#  Copy this file to .env and fill in your values
#  NEVER commit .env to git (it's in .gitignore)
# ══════════════════════════════════════════════════════

# Backend API URL — set this to your Render.com URL after deploying
# Example: https://climatehealth-api.onrender.com
VITE_API_URL=http://localhost:8000

# Optional: OpenAQ API key for real AQI data (free at openaq.org)
VITE_OPENAQ_API_KEY=

# Optional: Mapbox token for district map layer (free tier available)
VITE_MAPBOX_TOKEN=
